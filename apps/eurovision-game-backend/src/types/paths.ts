export enum RootPaths {
	VOTES = "votes",
	ADMIN = "admin",
	GROUPS = "groups",
	USERS = "users",
}

export enum AuthPaths {
	AUTH = "auth",
	LOGIN = "login",
	LOGOUT = "logout",
	SIGN_UP = "signUp",
	ROLES = "roles",
}

export enum AdminPaths {
	PROMOTE = "promote",
	SUBMIT_FINAL = "submit-final",
}
