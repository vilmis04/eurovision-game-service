export const styles: ISx = {
	container: ({ spacing }) => ({
		padding: spacing(2, 0, 6, 0),
	}),
	field: {
		alignItems: "center",
		justifyContent: "center",
	},
	countryPoints: ({ spacing }) => ({
		padding: spacing(1, 0),
		alignItems: "center",
		justifyContent: "center",
	}),
};
